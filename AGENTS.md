# 项目约定（AI 助手必须遵守）

> 本文件是 AI 助手在本仓库工作时的最高优先级规则。每次开始工作前必须阅读并遵守。

## ⚠️ 铁律：未经用户确认，禁止推送到 GitHub

**"推给你"绝对不等于"推送到 GitHub 线上"。**

- `推给你` = 在本地启动测试服务器（`node scripts/local-server.js`），把预览链接发给用户确认效果
- `推送到 GitHub / 上线` = 用户明确说"可以 / 确认 / 上线 / 推送"之后，才允许执行

**任何未经用户确认的线上推送，都是违规操作。** 用户已多次强调此规则，违反一次就是严重错误。

## 标准工作流程

1. **本地修改**：所有改动只在本地工作区进行
2. **本地测试**：启动本地服务器，发预览链接给用户
3. **用户确认**：等用户明确说 OK / 可以 / 确认
4. **上线推送**：仅在用户确认后，直接推送到 `main` 分支（不要创建 PR、不要搞分支状态）

## 触发词对照

| 用户说 | 正确动作 | 禁止动作 |
|--------|----------|----------|
| "推给你 / 发我看看 / 看一下" | 启动本地服务器，发预览链接 | 推送到 GitHub |
| "上线 / 推送 / 确认 / 可以" | 推送到 GitHub main 分支 | 提前推送 |
| "我看看效果 / 改一下" | 本地改 + 本地测试 | 直接 commit/push |

## 详细流程

完整流程见 `docs/工作流程SOP.md`，本文件是核心规则的速查版。
