# 同步/维护脚本目录
# raw/ 为拉取飞书后生成的中间原始数据，可随时用 dump-tables.py 重新生成，不入库
scripts/raw/
__pycache__/
*.pyc

# 工作流设计草稿/临时预览文件，不入库
.superpowers/
diary-fusion.html
diary-preview.html

# 本地测试数据
# 说明：data/ 是线上数据库源（12 个 json 已跟踪为线上基线，不可删除）。
#     此规则仅拦截“本地测试新增的未跟踪文件”，防止误提交。
#     对已跟踪文件请在 upload.sh 中通过 ':(exclude)data' 排除（已配置）。
#     线上数据库更新走专用路径，见 scripts/upload.sh --with-data。
data/